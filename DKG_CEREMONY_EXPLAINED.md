# DKG Ceremony คืออะไร? (Distributed Key Generation)

## คำจำกัดความ

**DKG (Distributed Key Generation)** คือ พิธีกรรม/กระบวนการ ที่ใช้สร้าง **กุญแจลับร่วมกัน (shared private key)** แบบกระจายศูนย์ โดยที่ไม่มีใครรู้กุญแจเต็ม!

---

## 🔐 เปรียบเทียบง่ายๆ

### แบบปกติ (ไม่ใช้ DKG)
```
คนถือกุญแจ: Alice (มีกุญแจเต็ม)
ปัญหา: ถ้า Alice หายไป = ระบบพัง
       ถ้า Alice โกง = ลายเซ็นปลอมได้
```

### แบบ DKG (Threshold Cryptography)
```
 validators 3 คน: Alice, Bob, Charlie
 แต่ละคนมี: "ส่วนของกุญแจ" (share) ไม่ใช่กุญแจเต็ม
 
 ต้องใช้ 2 คน (threshold) ร่วมกันถึงจะเซ็นได้
 ถ้า Charlie หายไป: Alice + Bob ยังเซ็นได้อยู่
 ถ้า Alice โกง: ไม่เป็นไร เพราะไม่มีกุญแจเต็ม
```

---

## 🎯 ทำไม Tempo ถึงใช้ DKG?

### ปัญหาของ Consensus แบบเดิม
- ถ้า validator คนหนึ่งถูกแฮก หรือ private key รั่วไหล → ระบบเสี่ยง
- ถ้าต้อง rotate key (เปลี่ยนกุญแจ) → ต้อง stop network

### DKG แก้ปัญหา
1. **No Single Point of Failure** - ไม่มีใครถือกุญแจเต็ม
2. **Dynamic Membership** - เพิ่ม/ลด validator ได้ระหว่างทำงาน
3. **Key Rotation** - เปลี่ยน key ทุก epoch โดยอัตโนมัติ

---

## ⚙️ หลักการทำงาน (BLS12-381 Threshold)

### 1. คณิตศาสตร์เบื้องหลัง
```
Polynomial: f(x) = a₀ + a₁x + a₂x² + ... + aₜxᵗ

- a₀ = private key (ที่ต้องการปกปิด)
- f(1), f(2), f(3) = shares สำหรับ validator แต่ละคน
- t = threshold (ต้องการ t+1 คนเพื่อ reconstruct key)
```

### 2. บทบาทใน Ceremony

| บทบาท | คำอธิบาย | ใน Tempo |
|-------|---------|----------|
| **Dealers** | สร้าง polynomial และแจก shares | Validators ปัจจุบัน |
| **Players** | รับ shares และรวมเป็น group key | Validators ปัจจุบัน + ใหม่ |
| **Syncers** | รอ sync chain ก่อน | Validators ใหม่ที่เพิ่งเข้า |

---

## 🔄 ขั้นตอน DKG Ceremony ใน Tempo

### Epoch N (ก่อนเปลี่ยน)

```
Block N*50 (เริ่ม epoch):
├── Dealers (validators ปัจจุบัน):
│   └── สร้าง public polynomial + private shares
│   └── ส่ง shares ให้ players ผ่าน P2P
│
├── Players (validators ปัจจุบัน + ใหม่):
│   └── รับ shares จาก dealers
│   └── ส่ง ack (acknowledgment) กลับ
│
└── Syncers (validators ใหม่ที่ยัง sync ไม่เสร็จ):
    └── รอรอบถัดไป
```

### Epoch N+1 (เปลี่ยน validator set)

```
Block (N+1)*50:
├── รวม shares จากทุก dealers → group polynomial
├── Validators ใหม่ได้รับ signing share
└── ทุกคนมี key ใหม่สำหรับ sign blocks!
```

---

## 📊 ตัวอย่างใน Network ของเรา

### Validator 2 เข้ามาใหม่

```
Epoch 4:
- Dealers: [Validator 1]
- Players: [Validator 1]
- Syncers: [Validator 2] ← ยัง sync อยู่

Epoch 5:
- Dealers: [Validator 1]  
- Players: [Validator 1, Validator 2] ← เข้าร่วมแล้ว!
- Syncers: [Validator 1, Validator 2]

Validator 2 ได้รับ signing share → กลายเป็น Signer
```

### Validator 3 เข้ามาใหม่

```
Epoch 36:
- Dealers: [Validator 1, Validator 2]
- Players: [Validator 1, Validator 2]  
- Syncers: [Validator 1, Validator 2, Validator 3]

Epoch 37:
- Dealers: [Validator 1, Validator 2]
- Players: [Validator 1, Validator 2, Validator 3] ← เข้าร่วม!
- Syncers: [Validator 1, Validator 2, Validator 3]

Validator 3 ได้รับ signing share → กลายเป็น Signer
```

---

## 🔑 ประโยชน์ของ DKG

### 1. Security
- **No Key Exposure** - ไม่มี validator คนไหนรู้ private key เต็ม
- **Threshold Security** - ต้องมี t+1 คนร่วมกันถึงจะ sign ได้ (ป้องกันการโกง)

### 2. Flexibility  
- **Dynamic Addition** - เพิ่ม validator ได้ตลอดเวลา
- **Dynamic Removal** - ลบ validator ได้โดยไม่ต้อง restart
- **Key Refresh** - เปลี่ยน key ทุก epoch โดยอัตโนมัติ (forward secrecy)

### 3. Decentralization
- **No Trusted Dealer** - ไม่ต้องมีคนกลางแจก key
- **Distributed Generation** - ทุกคนร่วมสร้าง key ด้วยกัน

---

## 🎓 สรุปง่ายๆ

| คำถาม | คำตอบ |
|-------|--------|
| DKG คืออะไร? | พิธีกรรมสร้างกุญแจร่วมกันแบบไม่มีใครรู้กุญแจเต็ม |
| ทำไมต้องใช้? | ความปลอดภัย + เพิ่ม validator ได้ตลอดเวลา |
| ใครเป็น dealers? | Validators ปัจจุบัน |
| ใครเป็น players? | Validators ทั้งหมด (เก่า + ใหม่) |
| ได้รับอะไร? | Signing share (ส่วนของกุญแจ) |
| ใช้เวลานานไหม? | 1-2 epochs (~5-10 นาที) |

---

## 📚 References

- `/root/tempo/docs/internal/network-reconfiguration.md`
- BLS12-381 Threshold Signatures: https://docs.rs/commonware-cryptography/
- Validator Config: `/root/tempo/crates/contracts/src/precompiles/validator_config.rs`

---

*สร้างเมื่อ: January 30, 2026*
